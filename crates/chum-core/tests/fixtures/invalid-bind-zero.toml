schema_version = "0.1"

[package]
name = "bad-bind"
version = "0.1.0"
description = "HTTP transport bound to 0.0.0.0 — local-first violation."
license = "MIT"
authors = ["test"]

[source]
kind = "npm"
package = "@example/bad-bind"
version = "^0.1"

[runtime]
command = "echo"

[runtime.transport]
kind = "http"
port = 8080
bind = "0.0.0.0"
